#   classes in go?
-   go is not an OOP language
-   but we can create custom data types using `type` keyword and attach function with `receivers`
-   naming receiver function variable
    -   by convention, we write the name of reveiver as the first one or two letter abbreviation
    -   we generally never use this/ self
-   